<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove(Directory.Build.props, $(MSBuildThisFileDirectory)..))" Condition="Exists($([MSBuild]::GetPathOfFileAbove(Directory.Build.props, $(MSBuildThisFileDirectory)..)))"/>

    <PropertyGroup>
        <RelativeRoot>$(MSBuildProjectDirectory.Replace('$(ComposeUIRepositoryRoot)\prototypes\avalon-shell\src\shell', '$(ComposeUIRepositoryRoot)\src\shell'))\</RelativeRoot>
    </PropertyGroup>

</Project>